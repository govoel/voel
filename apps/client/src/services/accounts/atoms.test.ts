import { describe, it } from 'vitest';

describe('activeAccountSessionAtom', () => {
  it.todo('subscribes to authClient.useSession and emits session changes');

  it.todo('does not resubscribe when AccountManager emits changes for the same auth client');
});

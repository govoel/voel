import type { ComponentType, ReactNode } from 'react';

export type TabScreenColumnComponent = ComponentType<{ readonly children: ReactNode }>;

export declare const TabScreenColumn: TabScreenColumnComponent;

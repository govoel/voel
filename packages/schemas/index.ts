import { v1 } from './v1';

/**
 * Centralized collection of validation schemas for the Voel application.
 *
 * Organized by API version to support schema evolution and backward
 * compatibility as the application grows.
 */
export const schemas = { v1 };

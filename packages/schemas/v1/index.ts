import { library } from './library';

/**
 * Version 1 schemas for the Voel API.
 *
 * Contains all validation schemas used by the v1 API endpoints,
 * organized by domain/feature area.
 */
export const v1 = { library };
